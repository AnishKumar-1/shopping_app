spring:
  datasource:
    url: jdbc:h2:mem:authentication_db
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update


  flyway:
    enabled: false

eureka:
  client:
    enabled: false
