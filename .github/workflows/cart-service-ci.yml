name: CART SERVICE CI
on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - main

jobs:
  build-test:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'temurin'

      - name: Give maven wrapper (mvnw) file permission to execute
        working-directory: cart-service
        run: chmod +x mvnw

      - name: Build & Test
        working-directory: cart-service
        env:
          SPRING_PROFILES_ACTIVE: test
        run: ./mvnw clean test

  build-docker:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'temurin'

      - name: Give maven wrapper (mvnw) file permission to execute
        working-directory: cart-service
        run: chmod +x mvnw

      - name: Build JAR
        working-directory: cart-service
        run: ./mvnw clean package

      - name: Build image
        working-directory: cart-service
        run: docker build -t cart-service:latest .
